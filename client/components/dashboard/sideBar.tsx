import { GuildFormated } from "@/lib/guilds";

export const SideBar = ({ currentGuild, guilds }: { currentGuild: GuildFormated|null, guilds: GuildFormated[]}) => {


    return (
        <div>
            Enter
        </div>
    );
}